/**
 * Twitter (X) summary_large_image — re-uses the same OG image art.
 * Next.js auto-wires <meta name="twitter:image">.
 */
export { default } from './opengraph-image'
export { runtime, alt, contentType, size } from './opengraph-image'
