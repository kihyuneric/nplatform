'use client'

// 다크모드 제거 — 라이트 단일 테마 (McKinsey White Paper)
// ThemeToggle 은 외부 import 호환을 위해 빈 컴포넌트 stub 으로 유지됩니다.

interface ThemeToggleProps {
  variant?: 'icon' | 'dropdown'
  className?: string
}

export function ThemeToggle(_props: ThemeToggleProps) {
  return null
}
